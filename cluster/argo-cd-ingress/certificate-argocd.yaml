apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-argo-lab
  namespace: argocd
spec:
  secretName: argocd-argo-lab-tls
  dnsNames:
    - argo.lab
  issuerRef:
    name: internal-ca           
    kind: ClusterIssuer