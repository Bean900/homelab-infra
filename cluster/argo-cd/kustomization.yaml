apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Ziel-Namespace für die Installation
namespace: argocd

resources:
  # HIER WIRD DIE VERSION GESTEUERT! 
  # Wenn du dieses Tag im Git änderst, aktualisiert sich Argo CD selbst.
  - https://github.com/argoproj/argo-cd//manifests/install.yaml?ref=v3.4.2