apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: games

resources:
  - namespace.yaml
  - persistent-volume.yaml
  - service.yaml
  - statefulset.yaml

labels:
  - includeSelectors: true
    pairs:
      app: minecraft
