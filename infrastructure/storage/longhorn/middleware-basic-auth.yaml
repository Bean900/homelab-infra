apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: longhorn-dashboard-auth
  namespace: longhorn-system
spec:
  basicAuth:
    secret: longhorn-auth # Name muss zum Makefile-Secret passen
    removeHeader: true